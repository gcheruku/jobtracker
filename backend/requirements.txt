fastapi>=0.110
uvicorn[standard]>=0.29
sqlmodel>=0.0.16
python-multipart>=0.0.9
pypdf>=4.0
python-dotenv>=1.0

# Gmail job-alert ingestion + Gemini scoring
google-api-python-client>=2.130
google-auth>=2.30
google-auth-oauthlib>=1.2
google-genai>=1.0
python-docx>=1.1
beautifulsoup4>=4.12
lxml>=5.2
requests>=2.31
pgeocode>=0.4
APScheduler>=3.10

# NOTE: offline semantic resume<->JD matching (sentence-transformers / Torch) is
# kept in requirements-semantic.txt so the base image stays small. Install it for
# the full build (Dockerfile arg WITH_SEMANTIC=true); the app degrades gracefully
# to "no semantic scoring" when it's absent.

# Optional: only needed if you set ANTHROPIC_API_KEY for real LLM analysis.
anthropic>=0.40
